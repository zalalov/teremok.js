/**
 * Full info page for orders, clients, tariffs etc.
 * @param el
 * @constructor
 */
T.UI.FullInfoPage = function (el) {
    var self = this;

    T.UI.BasicElement.call(this, el);
};

T.UI.FullInfoPage.prototype = Object.create(T.UI.BasicElement.prototype);
